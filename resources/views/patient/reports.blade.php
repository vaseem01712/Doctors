<x-portal-shell title="Medical Reports">
<div class="mb-7"><span class="section-label">MEDICAL REPORTS</span><h1 class="section-heading !mt-3 !text-4xl">Your secure reports</h1><p class="mt-3 text-slate-500">Only documents belonging to your account can be accessed.</p></div>
<div class="grid gap-4 md:grid-cols-2">@forelse($reports as $r)<article class="premium-card p-6"><div class="flex items-start justify-between gap-4"><div><p class="text-lg font-extrabold text-navy-900">{{$r->title}}</p><p class="mt-1 text-sm text-slate-500">{{$r->test_type ?: 'Medical test'}} · {{$r->report_date->format('d M Y')}}</p><p class="mt-1 text-sm text-slate-500">Dr. {{$r->doctor->name}}</p></div><span class="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">Sent</span></div>@if($r->description)<p class="mt-4 text-sm leading-6 text-slate-600">{{$r->description}}</p>@endif<div class="mt-5 flex items-center justify-between"><span class="text-xs font-semibold text-slate-400">{{number_format($r->file_size/1024,0)}} KB · {{strtoupper(pathinfo($r->file_name,PATHINFO_EXTENSION))}}</span><a href="{{route('medical-reports.download',$r)}}" class="btn-primary !px-4 !py-2.5">View / Download</a></div></article>@empty<div class="soft-panel p-14 text-center md:col-span-2"><p class="text-lg font-extrabold text-navy-900">No medical reports yet.</p><p class="mt-2 text-sm text-slate-500">Reports sent by your doctor will appear here.</p></div>@endforelse</div><div class="mt-5">{{$reports->links()}}</div>
</x-portal-shell>
