<x-portal-shell title="Appointments">
<div class="mb-7"><span class="section-label">APPOINTMENTS</span><h1 class="section-heading !mt-3 !text-4xl">Your schedule</h1><p class="mt-3 text-slate-500">Today's, upcoming and historical appointments for your practice.</p></div>
<form class="mb-5 grid gap-3 sm:grid-cols-[1fr_180px_140px_auto]"><input name="q" value="{{request('q')}}" class="input-field" placeholder="Search patient"><select name="status" class="input-field"><option value="">All statuses</option>@foreach(['pending','confirmed','completed','cancelled'] as $s)<option value="{{$s}}" @selected(request('status')===$s)>{{ucfirst($s)}}</option>@endforeach</select><input name="date" value="{{request('date')}}" type="date" class="input-field"><button class="btn-primary">Filter</button></form>
<div class="soft-panel overflow-hidden"><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs font-extrabold uppercase text-slate-400"><tr><th class="px-5 py-4">Patient</th><th class="px-5 py-4">Date / time</th><th class="px-5 py-4">Status</th><th class="px-5 py-4"></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($appointments as $a)<tr><td class="px-5 py-5 font-bold">{{$a->patient?->name ?? $a->patient_name}}</td><td class="px-5 py-5">{{$a->appointment_date->format('d M Y')}} · {{substr($a->appointment_time,0,5)}}</td><td class="px-5 py-5">{{ucfirst($a->status)}}</td><td class="px-5 py-5 text-right">@if($a->patient)<a class="font-bold text-primary-700" href="{{route('doctor.patient.show',$a->patient)}}">Patient →</a>@endif</td></tr>@empty<tr><td colspan="4" class="px-5 py-14 text-center text-slate-500">No appointments found.</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-100 p-4">{{$appointments->links()}}</div></div>
</x-portal-shell>
